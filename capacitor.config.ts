import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'me.mmkmou.isbn_finder',
  appName: 'Book ISBN Finder',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
