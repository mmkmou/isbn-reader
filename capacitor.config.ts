import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Book ISBN Finder',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
